# 🤖 AI Agent 개발 원칙 및 협업 규칙 (AGENTS.md)

이 문서는 이 프로젝트 및 이 템플릿을 기반으로 파생된 모든 프로젝트에서 AI 에이전트가 반드시 준수해야 하는 공통 개발 규칙입니다.

---

## 0. 새 프로젝트 인계 및 기능 개발 표준 라이프사이클 (Zero-to-One Loop)

새로운 프로젝트를 인계받거나 새로운 기능을 개발할 때, AI 에이전트는 반드시 아래의 **4단계 행동 루프**를 엄격히 준수해야 합니다.

```text
[1. 기획 (Plan)] ──> [2. 승인 (Approve)] ──> [3. 구현 (Execute)] ──> [4. 검증 및 보고 (Verify)]
```

1. **1단계: 기획 (Planning)**:
   - 사용자의 요구사항을 분석하고, **코드를 수정하기 전에 먼저 `implementation_plan.md`를 작성**합니다.
   - 계획서에는 반드시 **`[도메인] ➡️ [기능] ➡️ 6단계 노드 파이프라인 표`**가 포함되어야 합니다.
   - 복잡한 전역 공유 상태나 깊은 드릴링이 필요한 경우 가벼운 전역 상태 라이브러리(`Zustand`) 도입을 먼저 제안합니다.
2. **2단계: 사용자 승인 (User Approval)**:
   - 계획서 작성 후 임의로 코드를 수정하거나 파일을 생성하지 않고, **사용자의 명시적 승인을 기다립니다.**
3. **3단계: 모듈화 구현 (Implementation)**:
   - 승인 후 코드를 작성하며, 신규/수정 파일 상단에 **표준 JSDoc 아키텍처 주석(`@domain`, `@feature`, `@phase` 등)**을 필수로 기재합니다.
   - 프리뷰가 필요한 UI 컴포넌트는 하단에 `MyComponent._previewProps = { ... }`를 선언하여 인스펙터 보드(5174)에서 Zero-config로 렌더링되도록 합니다.
4. **4단계: 기술 무결성 검증 및 보고 (Verification & Report)**:
   - 구현 완료 후 아래 명령어로 100% 자동 검증을 통과해야 합니다:
     - `npm run lint` (코드 및 아키텍처 주석 누락 0건)
     - `npm run test:run` (연산/판단 단위 테스트 100% 통과)
     - `npm run build` (TypeScript 및 번들링 성공)
     - `npm run check:headless` (헤드리스 브라우저 콘솔 오류 0건)
   - 검증 결과를 `walkthrough.md`에 기록하고, 수동 사용성 검증은 사용자에게 정중히 인계합니다.

---

## 1. 언어 및 문서화 규칙 (Language & Documentation)

- **모든 계획 및 결과 문서는 반드시 한국어로 작성합니다.**
  - `implementation_plan.md`, `walkthrough.md`, 그리고 사용자에게 전달하는 모든 진행 보고 및 응답은 명확하고 친절한 한국어로 서술합니다.
  - 코드 주석 및 커밋 메시지도 한국어를 기본으로 하거나 프로젝트 관례에 맞게 일관성 있게 작성합니다.
  - 텍스트 인코딩은 항상 **UTF-8 (LF)**을 유지하여 한글 깨짐을 방지합니다.

- **계획서(`implementation_plan.md`)의 6단계 노드 파이프라인 문법 의무화**:
  - 계획서 작성 시 단순한 파일 변경 나열을 금지하며, 반드시 **독립된 개별 기능(@feature) 단위로 `[입력 ⚡ ➡️ 판단 ⚖️ ➡️ 연산 ⚙️ ➡️ (API 🌐) ➡️ 저장소 💾 ➡️ 출력 🎨]`의 순서열 파이프라인 표**를 작성해야 합니다.
  - **1개 기능(@feature)의 기준**: 사용자의 1개 시작점(Trigger)에서 출발하여 1개의 최종 결과(Render)로 도달하는 하나의 완결된 라이프사이클 체인 (시작 버튼이 다르거나 저장소가 다르면 별개의 기능으로 쪼갤 것).
  - 필수 포함 열: `순서`, `단계 (@phase)`, `담당 파일 (구현체)`, `요소 본질 (@target)`, `세부 역할 및 흐름 설명`
  - 상세 가이드 및 안티패턴 방지 지침은 [docs/ARCHITECTURE_PIPELINE_GUIDE.md](./docs/ARCHITECTURE_PIPELINE_GUIDE.md)를 참조합니다.


---

## 2. 버전 관리 및 배포 안전 규칙 (Git Safety)

- **`git push`는 반드시 사용자의 명시적인 사전 승인을 받은 후 실행합니다.**
  - `git commit`이나 브랜치 작업은 자율적으로 수행하더라도, 원격 저장소로의 `git push`는 변경된 커밋 요약 및 대상 브랜치를 안내하고 사용자의 동의를 얻은 후 실행합니다.
  - 임의로 `push --force`를 수행하지 않습니다.

---

## 3. 컴포넌트 중심 개발 기조 (Component-Driven Architecture)

- **UI 컴포넌트와 비즈니스 로직 컴포넌트의 명확한 분리**
  - **Presentational (UI)**: 스타일과 화면 표현, 사용자 입력 전달만을 담당하는 순수 UI 컴포넌트 (`src/components/ui/`)
  - **Logical / Headless (Hooks & Containers)**: 복잡한 상태 관리, API 통신, 데이터 변환 등 비즈니스 로직을 담당하는 커스텀 훅 (`src/hooks/`) 및 컨테이너 컴포넌트
  - 거대한 단일 컴포넌트 파일 안에 수많은 `useState`, `useEffect`, 핸들러, 대규모 JSX가 뒤섞이지 않도록 관심사를 분리합니다.

- **컴포넌트 파일 기반 아키텍처 지향**
  - 기능 단위로 컴포넌트를 모듈화하여 독립적으로 테스트 및 재사용이 가능하도록 구성합니다.
  - 공통 유틸리티는 `src/lib/`에 배치하고, 경로 별칭(`@/*`)을 적극 활용합니다.

- **전역 상태 관리 라이브러리(Zustand 등) 제안 기준**:
  - 이 템플릿은 초경량 유지를 위해 기본적으로 React 내장 훅(`useState`, `useContext`) 및 `localStorage`로 출발합니다.
  - 기획/계획 단계에서 **도메인 간 공유 데이터가 많아지거나, Props 드릴링이 깊어지거나, 복잡한 게임 상태/인벤토리/전역 세션 등이 필요하다고 판단될 경우**, AI 에이전트는 `implementation_plan.md` 작성 시 가볍고 직관적인 전역 상태 관리 라이브러리(예: `Zustand`)의 도입 및 설치를 사용자에게 적극 제안/추천해야 합니다.


---

## 4. 코드 분리 및 응집도 가이드 (Feature-Driven Cohesion & 300 Lines Advisory)

- **기계적인 300줄 제한이 아닌, '기능 중심의 응집도'를 최우선으로 합니다.**
  - 300줄 초과는 기계적으로 코드를 잘라내기 위한 제약이 아니라, **"한 파일 안에 두 가지 이상의 독립적인 기능이나 관심사가 뒤섞여 비대해진 것은 아닌가?"**를 점검하기 위한 권고 지표(Advisory)입니다.
  - **분리 검토 대상**:
    - 하나의 컴포넌트 파일 안에 UI 레이아웃, 복잡한 비즈니스 계산, 외부 API 통신, 서브 뷰 등이 혼재되어 300줄이 넘어가는 경우 ➡️ 기능 단위 서브 컴포넌트나 커스텀 훅(`src/hooks/`)으로 분리합니다.
  - **단일 파일 유지 대상**:
    - 하나의 기능/도메인으로서 응집도가 매우 높아 한 파일에 모여 있는 것이 흐름 파악과 유지보수에 훨씬 유리하다고 판단되는 경우 ➡️ 억지로 파일을 쪼개지 않고 300줄 초과를 허용합니다.
    - 경고를 무시하고 단일 파일로 유지할 명확한 근거가 있는 경우, 파일 상단에 `// @cohesive-file` 주석을 표기할 수 있습니다.

---

## 5. 품질 검증 및 런타임 체크 (Quality Assurance)

- **아키텍처 주석 자동 검증 (`npm run lint:arch`)**:
  - `src/` 내 모든 신규/수정 소스 파일은 필수 아키텍처 주석(`@domain`, `@feature`, `@phase`)이 누락되지 않아야 합니다.
  - `npm run lint` 실행 시 코드 린트(`oxlint`)와 함께 아키텍처 린트(`npm run lint:arch`)가 자동으로 함께 수행되어 누락을 기계적으로 방지합니다.

- **단위 테스트 자동 검증 (`npm run test:run`)**:
  - '판단(Decision)' 및 '연산(Compute)' 단계에 해당하는 복잡한 비즈니스 로직, 순수 함수, 커스텀 훅은 Vitest 기반 단위 테스트(`*.test.ts`)를 적극 작성하여 검증합니다.

- **수동 검증 사용자 전담 원칙**:
  - 실제 인터랙션 체감, 디자인 감각, 세부 시나리오 테스트 등의 **수동 검증(Manual Verification)은 사용자가 직접 수행**합니다.
  - 에이전트는 코드 수정 후 자동화된 빌드(`npm run build`), 린트(`npm run lint`), 단위 테스트(`npm run test:run`), 그리고 필요 시 헤드리스 브라우저를 통한 콘솔 에러 유무 등 '기술적 무결성'만을 확인하여 사용자에게 보고합니다.

- **라이브 프리뷰 목업 선언 (`_previewProps`)**:
  - `src/components/` 내에 Props가 필수적인 컴포넌트를 작성할 경우, 컴포넌트 하단에 `MyComponent._previewProps = { ... }`를 선언하면 인스펙터 보드(포트 5174)에서 Zero-config로 실물 프리뷰가 즉시 렌더링됩니다.


---

## 6. 기능별 6단계 노드 파이프라인 및 파일 주석 표준 (Architecture Pipeline Annotations)

AI 에이전트와 개발자가 바이브 코딩 시 시스템 멘탈 모델과 파일 연결성을 잃지 않도록, **새로운 파일을 생성하거나 주요 로직을 수정할 때는 반드시 파일 최상단에 표준 JSDoc 메타데이터 주석을 포함**합니다.

- **표준 주석 템플릿**:
  ```tsx
  /**
   * @domain <상위 도메인/모듈명> (예: 레이아웃 & 테마, 사용자 인증, 인벤토리 시스템)
   * @feature <소속 기능명> (예: 테마 전환, 전투 시스템, 공통 유틸리티)
   * @phase <6단계 중 하나> (입력 | 판단 | 연산 | API | 저장소 | 출력)
   * @target <요소 본질> (예: Sun/Moon 토글 버튼)
   * @trigger <작동 방식> (예: 클릭, 타이머, 마운트)
   * @desc <인간이 이해하기 쉬운 1~2줄 한글 역할 및 책임 설명>
   * @store <선택사항: 연동 저장소명 (write/read)> (예: localStorage (write: theme))
   * @next <선택사항: 다음 단계로 이어지는 대상 파일 경로>
   */
  ```

- **계층 구조 원칙 (Hierarchy)**:
  1. **도메인/모듈 (`@domain` / `@module`)**: 기능들을 묶어주는 거시적 업무/화면 영역 (예: `레이아웃 & UI`, `인터랙션 데모`)
  2. **기능 (`@feature`)**: 사용자의 1개 시작점(Trigger)에서 출발하여 1개의 최종 결과(Render)로 도달하는 완결된 라이프사이클 단위 (예: `테마 전환`, `AI 프롬프트 복사`)
  3. **노드 (`@phase`)**: 해당 기능 체인을 구성하는 6단계 파이프라인 블록

- **6단계 노드 분류 기준**:
  1. **입력 (Trigger)**: 사용자 클릭, 폼 입력, URL 라우트 진입, 타이머, 마운트 이벤트
  2. **판단 (Decision)**: 조건문, 권한 검사, 유효성 검증, 상태 분기 가드
  3. **연산 (Compute)**: 비즈니스 계산 공식, 상태 반전/변환, 순수 데이터 가공 함수
  4. **API (Network)**: 백엔드 서버 통신, 외부 서비스/결제/웹소켓 연동
  5. **저장소 (Store)**: 로컬스토리지, 전역 스토어(Zustand/Redux), 캐시 쓰기/읽기
  6. **출력 (Render)**: JSX DOM 렌더링, Tailwind 스타일 반영, 알림/토스트/사운드 피드백

- 이 주석은 관리자 뷰(`http://localhost:5174/`)의 **'기능 체인 뷰'**와 **'저장소 입출력 허브'**에 실시간으로 파싱되어 자동 반영됩니다.


